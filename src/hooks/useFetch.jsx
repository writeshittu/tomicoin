import { useEffect, useState } from "react";

const API_KEY = import.meta.env.VITE_IPHY_API


const usefetch=()=>{
const [gifUrl, setgifUrl] = useState('')
// const getGif
}
useEffect(() => {
    usefetch()
}, [dependencies])